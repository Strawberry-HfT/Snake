<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.hft.stuttgart.strawberry.snake" >

    <!-- Berechtigung für  Bluetooth -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_berry"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <!-- Activities -->
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GPSingleActivity" />

        <supports-screens
            android:anyDensity="true"
            android:largeScreens="true"
            android:normalScreens="true"
            android:resizeable="true"
            android:smallScreens="true"
            android:xlargeScreens="true" />
        <!-- android:requiresSmallestWidthDp="integer" -->
        <!-- android:compatibleWidthLimitDp="integer" -->
        <!-- android:largestWidthLimitDp="integer" -->

        <activity
            android:name="de.hft.stuttgart.strawberry.activities.BluetoothActivity"
            android:label="@string/title_activity_bluetooth_list" >
        </activity>
    </application>

</manifest>
